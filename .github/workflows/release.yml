name: Build library

on:
  push:
    tags:
      - 'v*'

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Configure CMake
        run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release
      - name: Build
        run: cmake --build ${{github.workspace}}/build

      - uses: actions/upload-artifact@v3
        with:
          name: lib
          path: |
            build/libEnquirer.a
            build/libEnquirer.so

#  build-windows:
#    runs-on: windows-latest
#    steps:
#      - uses: actions/checkout@v3
#
#      - name: Configure CMake
#        run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release
#      - name: Build
#        run: cmake --build ${{github.workspace}}/build
#
#      - uses: actions/upload-artifact@v3
#        with:
#          name: lib
#          path: |
#            build/libEnquirer.dll

#  build-macos:
#    runs-on: macos-latest
#    steps:
#      - uses: actions/checkout@v3
#
#      - name: Configure CMake
#        run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=Release
#      - name: Build
#        run: cmake --build ${{github.workspace}}/build
#
#      - uses: actions/upload-artifact@v3
#        with:
#          name: lib
#          path: |
#            build/libEnquirer.*